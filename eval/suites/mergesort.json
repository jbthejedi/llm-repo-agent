{
  "name": "example_suite",
  "description": "Example evaluation suite for testing the eval harness",
  "defaults": {
    "test_cmd": "python -m pytest -q"
  },
  "tasks": [
    {
      "task_id": "fix_mergesort",
      "repo": "~/projects/QuixBugs",
      "goal": "Fix the mergesort implementation in python_programs/mergesort.py so that the tests pass",
      "test_cmd": "python -m pytest -q python_testcases/test_mergesort.py",
      "metadata": {
        "difficulty": "medium",
        "category": "sorting",
        "expected_files": ["python_programs/mergesort.py"]
      }
    }
  ]
}
